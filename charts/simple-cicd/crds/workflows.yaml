apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: workflows.simple-cicd.jlsalvador.online
spec:
  group: simple-cicd.jlsalvador.online
  names:
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    shortNames:
    - w
    singular: workflow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: When is true, the execution of this workflow will be skipped.
      jsonPath: .spec.suspend
      name: Suspend
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workflow is the Schema for the workflows API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: WorkflowSpec defines the desired state of Workflow
            properties:
              jobsToBeCloned:
                description: >
                  Jobs to be cloned when this Workflow is executed. Each entry
                  may reference a Job in a different namespace; when namespace is
                  omitted the Workflow's own namespace is used.
                items:
                  properties:
                    name:
                      type: string
                    namespace:
                      description: >
                        Namespace of the Job template. Defaults to the Workflow's
                        namespace when omitted.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              next:
                description: Optional list of Workflows to execute next
                items:
                  properties:
                    name:
                      description: Workflow name
                      type: string
                    namespace:
                      description: >
                        Namespace of the next Workflow. Defaults to the current
                        Workflow's namespace when omitted.
                      type: string
                    when:
                      default: Always
                      description: "Describes the conditions for when the next Workflow will run."
                      enum:
                      - OnSuccess
                      - OnAnySuccess
                      - OnFailure
                      - OnAnyFailure
                      - Always
                      type: string
                  required:
                  - name
                  type: object
                type: array
              suspend:
                default: false
                description: When is true, the execution of this workflow will be
                  skipped. Defaults to false.
                type: boolean
            required:
            - jobsToBeCloned
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
