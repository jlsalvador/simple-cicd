---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: workflowwebhooks.simple-cicd.jlsalvador.online
spec:
  group: simple-cicd.jlsalvador.online
  names:
    kind: WorkflowWebhook
    listKind: WorkflowWebhookList
    plural: workflowwebhooks
    shortNames:
    - ww
    singular: workflowwebhook
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkflowWebhook is the Schema for the workflowwebhooks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WorkflowWebhookSpec defines the desired state of WorkflowWebhook
            properties:
              concurrencyPolicy:
                description: "Specifies how to treat concurrent executions of a job
                  that is created by this Workflow. If not specified, the default
                  behavior is \"Allow\". \n The spec may specify only one of the following
                  concurrency policies: - Allow: Allows concurrently running jobs.
                  - Forbid: If it is time for a new job run and the previous job run
                  hasn't finished yet, skips the new job run. - Replace: If it is
                  time for a new job run and the previous job run hasn't finished
                  yet, replaces the currently running job run with a new job run."
                enum:
                - Allow
                - Forbid
                - Replace
                type: string
              suspend:
                description: Defaults to false
                type: boolean
              workflows:
                items:
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - workflows
            type: object
        type: object
    served: true
    storage: true
